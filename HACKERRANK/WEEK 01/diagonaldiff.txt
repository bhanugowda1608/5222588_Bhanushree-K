int diagonalDifference(int arr_rows, int arr_columns, int** arr) {int sum_primary = 0;
    int sum_secondary = 0;

    for (int i = 0; i < arr_rows; i++) {
        sum_primary += arr[i][i];
        sum_secondary += arr[i][arr_rows - 1 - i];
    }

    return abs(sum_primary - sum_secondary);
}

